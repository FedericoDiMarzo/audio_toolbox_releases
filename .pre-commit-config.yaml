# .pre-commit-config.yaml
repos:
-   repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.3
    hooks:
    -   id: clang-format
        # This ensures it hits .cpp, .hpp, .cc, .h, etc.
        types_or: [c++, c]
        exclude: ^(libs/|build/)